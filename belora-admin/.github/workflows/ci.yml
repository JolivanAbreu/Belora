name: Belora Admin - CI

on:
  push:
    branches: [main]
    paths:
      - "belora-admin/**"
  pull_request:
    paths:
      - "belora-admin/**"

defaults:
  run:
    working-directory: belora-admin

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: belora-admin/package-lock.json

      - name: Instalar dependências
        run: npm ci

      - name: Rodar testes de componente
        run: npm test

      - name: Build de produção
        run: npm run build

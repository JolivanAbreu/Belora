server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: qualquer rota (ex.: /agenda, /clientes) deve cair no index.html
    # e deixar o React Router decidir o que renderizar.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
